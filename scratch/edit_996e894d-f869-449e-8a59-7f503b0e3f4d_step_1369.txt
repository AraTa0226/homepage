"            // Use updateCategory for each changed category (avoids reorderPlans race condition)\n            plans.forEach(cat => {\n                if (cat.type !== 'audio') return;\n                const nextCat = nextPlans.find((p: any) => p.id === ca
<truncated 788 bytes>